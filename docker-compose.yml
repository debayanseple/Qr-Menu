services:
  postgres:
    image: postgres:16-alpine
    container_name: qr-ordering-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-qr_app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-qr_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-qr_ordering}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-qr_app}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
